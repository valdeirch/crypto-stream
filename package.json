{
  "name": "crypto-stream",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix service run start\" \"npm --prefix web run dev\" --names \"service,web\" --prefix-colors \"cyan,magenta\" --kill-others-on-fail",
    "install:all": "npm --prefix service install && npm --prefix web install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
